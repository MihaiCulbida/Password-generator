# Password Generator
A simple, lightweight web app for generating secure random passwords builded on HTML, CSS, and JavaScript.

# Features
- Generate passwords between 4 and 50 characters
- Uses a mix of uppercase, lowercase, digits, and special characters
- One-click copy to clipboard
- Clean UI
